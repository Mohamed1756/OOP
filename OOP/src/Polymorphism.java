public class Polymorphism {

    //POLYMORPHISM

    // more than one function, with same name yet different use.

    void method (int a){
        // only takes one number.
    }

    void method (int a, int b){
        // takes two nums.
    }

    void method (int a, int b, int c){
        // takes three nums and so on.....
    }


    // CONSTRUCTOR EXAMPLE:

    public void Polymorphism(String name){
     /*   Member function of the class (Bearing same name as the class).
        Automatically called, when object is initialised.
                Constructor has no return type. */
    }

}
